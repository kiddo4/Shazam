import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Colors.yellow;
  static const Color background = Colors.black;
  static const Color surface = Color(0xFF1C1C1C);
  static const Color text = Colors.white;
  static const Color textSecondary = Colors.grey;

  // Animation colors
  static const Color waveColor = Colors.yellow;
  static const Color waveBackgroundColor = Color(0xFF2C2C2C);

  // UI Element colors
  static const Color buttonBackground = Colors.yellow;
  static const Color buttonText = Colors.black;
  static const Color cardBackground = Color(0xFF242424);

  // Status colors
  static const Color success = Colors.green;
  static const Color error = Colors.red;
  static const Color listening = Colors.yellow;
}
